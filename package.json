{
  "name": "@oada/cerebral-module",
  "version": "0.9.0",
  "description": "oada cerebral module",
  "main": "./index.js",
  "scripts": {
    "test": "mocha -w --require babel-polyfill --compilers js:babel-register test/**/*.test.js",
    "build": "babel src --out-dir ./ --presets=env",
    "prepare": "npm run build",
    "build-watch": "babel --watch src --out-dir ./ --presets=env",
    "dev": "cpx --watch --verbose './sequences.js' '../TrialsTracker/node_modules/@oada/cerebral-module/sequences.js'"
  },
  "author": "oada",
  "license": "Apache-2.0",
  "dependencies": {
    "@oada/cerebral-provider": "^1.9.15",
    "assert": "^1.4.1",
    "babel-polyfill": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "babel-preset-latest": "^6.24.1",
    "babel-preset-stage-3": "^6.24.1",
    "babel-register": "^6.26.0",
    "cerebral": "^4.2.2",
    "chai": "^4.1.2",
    "fs": "^0.0.1-security",
    "url": "^0.11.0"
  }
}
